name: workflow
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18]

    steps:
        - uses: action/checkout@v6
        - uses: actions/setup-node@v6
        - run: make setup
        - run: make test
        - run: make lint